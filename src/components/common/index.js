// import Vue from 'vue'

// // 全局注册组件
// const requireComponent  = require.context('./', false,  /(index\.js|index\.vue)$/)

// requireComponent.keys().forEach( fileName => {
//     console.log(fileName)

//     if (fileName === './index.js') return
//     const componentName = requireComponent(fileName).default
//     Vue.component(componentName.name, componentName)
// })
